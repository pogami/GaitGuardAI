import SwiftUI

@main // This 'at' symbol is the "main" the error is looking for!
struct GaitGuardAI_Watch_AppApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
